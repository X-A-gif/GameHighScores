<div class="row">
  <div class="col-auto">
    <h2>Welcome {{user.name}} !! to the Arcade !</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Check out the Highest Scores !!</h3>

    <table id="table" >
<tr>
    <th>UserID</th>
    <th>UserName</th>
    <th>HighScore</th>
</tr>

{{#each users as |user| }}


<tr>
    <td>{{user.id}}</td>
    <td>{{user.name}}</td>
    <td>{{user.max_score}}</td>
</tr>
{{/each}}
</table>

  </div>    
</div>
  
<script src="./js/profile.js"></script>
